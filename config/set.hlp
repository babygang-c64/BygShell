 
set : SET VARIABLE VALUE

SET ["]['][VARIABLE_NAME]=[VALUE][']["]

STRING MODIFIERS ARE EXPANDED ONLY WHEN
USING THE VARIABLE CONTENT.
DOUBLE QUOTE ALLOWS CONTENT WITH SPACES
SINGLE QUOTE PREVENTS STRING EXPANSION

STRING MODIFIERS:

%%R<REGISTER#> : REGISTER (R0 TO R7) VALUE
IN HEXADECIMAL
%%P<REGISTER#> : PSTRING AT REGISTER ADDRESS
%%V<VARIABLE NAME>%% : VARIABLE VALUE
%%C<COLOR #> : COLOR CHANGE CHARACTER
(COLOR # IS HEX NIBBLE)
%%%% : %% SIGN

EXAMPLE :

SET PROMPT=%%C2%%VDEVICE%%%%C5>

